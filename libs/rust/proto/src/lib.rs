pub mod generated {
    // OUT_DIR generated files (prost)
    include!(concat!(env!("OUT_DIR"), "/swarm.common.rs"));
    // Additional includes will appear automatically as prost generates files for each package
}
